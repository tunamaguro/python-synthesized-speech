services:
  dev:
    build:
      context: .
    command: sleep infinity
    volumes:
      - type: bind
        source: .
        target: /home/vscode/workspaces
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [ gpu ]
